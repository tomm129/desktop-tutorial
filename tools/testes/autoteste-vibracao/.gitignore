# Artefatos de build do ESP-IDF -- gerados, nunca versionados. Sao 161 MB.
build/
# sdkconfig e derivado de sdkconfig.defaults + set-target. Versionar traria
# o target de quem compilou por ultimo junto.
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
