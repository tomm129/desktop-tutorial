# Artefatos de build do ESP-IDF -- gerados, nunca versionados.
build/
# sdkconfig e derivado de sdkconfig.defaults + set-target. Versionar traria
# o target de quem compilou por ultimo junto, e o proximo compilaria para o
# chip errado sem perceber -- que foi exatamente o que aconteceu aqui.
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
